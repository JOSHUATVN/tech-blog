{{#if posts.length}}
<section>
    <h2>Your Posts</h2>
    {{#each posts as |post| }}
        {{> dashboard-postinfo post}}
    {{/each}}
</section>
{{/if}}


<button type="submit" class="button">
    <a class="link" href="/dashboard/new">ADD NOEW POST</a>
</button>

<script src="/public/javascript/addpost.js"></script>